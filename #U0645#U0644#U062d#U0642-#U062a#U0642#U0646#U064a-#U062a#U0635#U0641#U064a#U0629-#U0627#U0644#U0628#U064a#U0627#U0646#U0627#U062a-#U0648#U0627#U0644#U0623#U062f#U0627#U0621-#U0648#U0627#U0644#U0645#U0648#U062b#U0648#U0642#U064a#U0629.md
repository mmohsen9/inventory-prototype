# ملحق تقني: بوابة التحقق، تسريع الإدخال، وموثوقية الربط مع Foodics
**يُقرأ مع:** وثيقة "تصميم نظام إدخال حركات المخزون" — الإصدار 1.0

---

## 1. بوابة تصفية البيانات والتحقق المسبق (Data Validation Gate)

الهدف: منع وصول أي عملية غير مكتملة أو غير متوافقة إلى Foodics API، لتفادي رفض الطلبات (خصوصاً خطأ 422 Unprocessable Entity) وتفادي إعادة العمل يدوياً.

### 1.1 مبدأ التصميم
تُضاف طبقة وسيطة اسمها **Validation Gate** تعمل في **3 نقاط توقف إلزامية**:

```
إدخال الموظف → [Gate 1: عند الحفظ كمسودة]
             → [Gate 2: عند إرسال المسودة للمراجعة]
             → [Gate 3: عند ضغط المحاسب "ترحيل إلى Foodics"]
```

لا تنتقل أي عملية للحالة التالية إن فشلت في أي بوابة، ويظهر للمستخدم سبب الفشل مباشرة بالعربية.

### 1.2 مطابقة الترميز (SKU / Units Matching)

| نوع الفحص | آلية التنفيذ | نتيجة الفشل |
|---|---|---|
| وجود SKU محلياً | مطابقة كود الصنف الممسوح مع جدول `items` المحلي المتزامن من Foodics | "الصنف غير موجود في القائمة المحلية — أرسل طلب صنف جديد" |
| تطابق الوحدة | مقارنة `storage_unit` المسجلة في العملية مع الوحدة المعتمدة في Foodics لنفس الصنف | "وحدة القياس المدخلة لا تطابق وحدة الصنف في فوديكس (المطلوب: {الوحدة الصحيحة})" |
| حداثة المزامنة | التحقق أن آخر مزامنة لقائمة الأصناف لم تتجاوز ساعة واحدة | "قائمة الأصناف قديمة، يُعاد المزامنة قبل المتابعة" |
| صلاحية الصنف في الموقع | التحقق أن الصنف مُفعّل فعلياً لهذا الفرع/المستودع في Foodics (وليس فقط موجوداً في القائمة العامة) | "هذا الصنف غير معرف في الفرع المستلم — يجب تفعيله في فوديكس أولاً" |
| اتساق معامل التحويل | التحقق أن `storage_to_ingredient_factor` المحلي مطابق للمسجل في Foodics | "معامل التحويل غير متطابق، راجع بيانات الصنف" |

> **آلية تقنية:** يُحتفظ بنسخة محلية محدثة (Cache) من جدول أصناف Foodics تتضمن لكل صنف: SKU، الوحدة، معامل التحويل، وقائمة الفروع التي يكون فيها الصنف مفعّلاً. تُحدَّث هذه النسخة عبر مهمة مجدولة (Scheduled Job) كل ساعة، وتُستخدم كمرجع فحص قبل أي إرسال، بدلاً من استدعاء Foodics API مباشرة عند كل حركة إدخال — لتقليل الحمل والوقت.

### 1.3 التحقق من المرفقات (Attachment Enforcement)

| القاعدة | التطبيق |
|---|---|
| لا حفظ نهائي بدون مرفق | زر "إرسال للمراجعة" و"ترحيل" يبقيان معطّلين (Disabled) حتى يكتمل رفع الملف فعلياً إلى Google Drive وإرجاع رابط صالح |
| التحقق من نجاح الرفع | النظام ينتظر تأكيد استجابة Google Drive (رابط + معرف ملف)، لا يكتفي بمجرد اختيار الملف من الجهاز |
| الحد الأدنى للمرفقات | فاتورة شراء: صورة واحدة على الأقل. تحويل: صورة اختيارية عند الإرسال، إلزامية عند وجود فرق في الاستلام |
| فشل الرفع (ضعف الشبكة) | تُحفظ الصورة مؤقتاً على الجهاز (Local Storage/IndexedDB) وتُعاد محاولة الرفع تلقائياً عند عودة الاتصال، مع شريط حالة "جاري رفع المرفق" |

### 1.4 جدول ملخص بوابات الفحص

| البوابة | فحوصات إلزامية |
|---|---|
| Gate 1 – حفظ كمسودة | تعبئة الحقول الأساسية (الموقع، التاريخ) فقط |
| Gate 2 – إرسال للمراجعة | مطابقة SKU + الوحدة لكل بند، رابط مرفق صالح، عدم وجود كمية صفرية أو فارغة |
| Gate 3 – ترحيل لـ Foodics | إعادة فحص فوري: الصنف ما زال مفعّلاً في الفرع، الوحدة ما زالت مطابقة (تحسباً لتعديل حدث بعد الاعتماد)، توفر رمز مصادقة API صالح (Token) |

---

## 2. تجربة إدخال فائقة السرعة (UX Optimization)

الهدف: تقليل زمن تسجيل الفاتورة أو التحويل إلى أقل عدد ممكن من اللمسات، خصوصاً على الجوال أثناء الحركة.

### 2.1 المسح المتتابع (Continuous Barcode Scanning)

**آلية العمل:**
1. يفتح الموظف "وضع المسح المستمر" مرة واحدة فقط في بداية العملية.
2. تبقى الكاميرا مفعّلة بدون إغلاق بين كل مسح والآخر.
3. عند كل عملية مسح ناجحة للصنف:
   - إن كان الصنف **غير موجود** في قائمة الفاتورة الحالية → يُضاف سطر جديد بكمية = 1 تلقائياً.
   - إن كان الصنف **موجوداً بالفعل** → تُزاد الكمية تلقائياً بمقدار +1 دون فتح لوحة مفاتيح أو لمس الشاشة.
4. تصدر الكاميرا/الجهاز نغمة صوتية ورجّة خفيفة (Vibration) قصيرة لتأكيد كل مسح، دون توقف الكاميرا.
5. يمكن للموظف في أي لحظة الضغط على أي سطر لتعديل الكمية يدوياً (مثلاً لإدخال كمية غير 1، كصندوق كامل).
6. زر "إنهاء المسح" يغلق الكاميرا وينقل المستخدم لشاشة المراجعة النهائية للفاتورة.

**فائدة تشغيلية مباشرة:** أمين المستودع يمسح 50 قطعة متتالية من نفس الصنف خلال ثوانٍ دون لمس الشاشة إطلاقاً، وهو ما يناسب طبيعة استلام الكراتين والعبوات المتكررة الظاهرة في ملف الأصناف الحالي (كرتون، شدة، بوكس...). [file:57]

### 2.2 القيم الافتراضية الذكية (Smart Defaults)

| الحقل | القيمة الافتراضية | المصدر |
|---|---|---|
| الموقع (Location) | يُعبأ تلقائياً حسب `branch_id` المرتبط بحساب المستخدم | جدول `users` |
| اسم الموظف | يُعبأ تلقائياً من الجلسة (Session) دون إدخال يدوي | تسجيل الدخول |
| التاريخ والوقت | الوقت الحالي للجهاز، قابل للتعديل فقط بصلاحية خاصة | ساعة النظام |
| نوع العملية الأخير | يُقترح نفس نوع آخر عملية نفّذها المستخدم (مثال: تحويل متكرر لنفس الفرع) | سجل آخر عملية |
| التكلفة/الوحدة | تُجلب تلقائياً من النسخة المحلية المتزامنة، غير قابلة للتعديل من الموظف (تعديلها صلاحية محاسب فقط) | Cache الأصناف |

**الأثر المتوقع:** تقليل عدد النقرات لكل فاتورة من نحو 8-10 نقرات تمهيدية إلى 1-2 نقرة فقط قبل بدء المسح الفعلي.

---

## 3. موثوقية الربط مع Foodics (API Resilience)

### 3.1 طابور الترحيل الخلفي (Background Sync Queue)

**المشكلة التي يعالجها:** فاتورة تحتوي 80 صنفاً، إذا أُرسلت دفعة واحدة وتوقف الاتصال في المنتصف، قد تُفقد الحالة أو تتكرر بعض البنود.

**الحل التقني:**
1. عند ضغط المحاسب "ترحيل إلى Foodics"، لا يُرسل الطلب مباشرة وينتظر الاستجابة في نفس الشاشة.
2. تُدرج العملية كاملة في **طابور مهام خلفي (Job Queue)** بحالة "قيد الترحيل".
3. تُعالج المهام في الطابور بندًا تلو الآخر (أو دفعات صغيرة حسب حدود Foodics API)، بمعدل يتوافق مع Rate Limit الخاص بـ Foodics.
4. تستمر واجهة المحاسب بالعمل الطبيعي دون تجمّد أثناء المعالجة، مع شريط تقدّم "جارٍ ترحيل 34 من 80 صنفاً".
5. عند فشل بند واحد فقط، يستمر الطابور في باقي البنود، ويُعزل البند الفاشل في قائمة "يحتاج إجراء" دون إيقاف العملية كاملة.
6. إعادة المحاولة التلقائية (Retry) 3 مرات بفاصل زمني متزايد (Exponential Backoff) قبل تصنيف البند كفاشل نهائياً.

**جدول حالات الطابور:**

| الحالة | المعنى |
|---|---|
| queued | بانتظار الدور |
| processing | قيد الإرسال الفعلي لـ Foodics |
| success | تم الترحيل وتأكيد Foodics |
| retrying | فشلت المحاولة، يُعاد تلقائياً |
| failed | فشلت بعد استنفاد المحاولات، تحتاج تدخل المحاسب |

### 3.2 معالجة الأخطاء الشائعة بلغة عربية واضحة

يُبنى **قاموس ترجمة أخطاء (Error Translation Map)** يحوّل استجابة Foodics التقنية إلى رسالة عملية واضحة، بدلاً من عرض كود الخطأ الخام للمحاسب.

| خطأ Foodics (تقني) | الترجمة المعروضة للمحاسب | الإجراء المقترح المعروض |
|---|---|---|
| 422 – Item not found in branch | الصنف غير معرّف في الفرع المستلم | فعّل الصنف في هذا الفرع داخل فوديكس، ثم أعد الترحيل |
| 422 – Unit mismatch | وحدة القياس غير متطابقة مع الصنف في فوديكس | راجع وحدة الصنف وصحّحها قبل إعادة الإرسال |
| 401 – Unauthorized | انتهت صلاحية الاتصال مع فوديكس | يلزم تجديد رمز الدخول (Token)، تواصل مع الدعم الفني |
| 429 – Too Many Requests | عدد الطلبات تجاوز الحد المسموح من فوديكس في نفس اللحظة | سيُعاد الإرسال تلقائياً خلال دقائق، لا حاجة لإجراء |
| 500 – Server Error | خطأ مؤقت من خادم فوديكس | سيُعاد المحاولة تلقائياً، إذا تكرر تواصل مع دعم فوديكس |
| Duplicate reference | هذه العملية مرحّلة مسبقاً لفوديكس | لا تُرسل مرة أخرى، راجع رقم المرجع {foodics_reference} |

**آلية العرض:** تظهر هذه الرسائل في لوحة "عمليات تحتاج إجراء" الخاصة بالمحاسب، مرتبة حسب الأولوية والتاريخ، مع زر "إعادة المحاولة" وزر "تعديل ثم إعادة الإرسال" لكل بند فاشل.

### 3.3 منع الازدواجية (Idempotency)

لكل عملية تُرسل لـ Foodics، يُولَّد **معرف فريد للطلب (Idempotency Key)** مرتبط برقم العملية الداخلي. إذا أُعيد إرسال نفس الطلب (بسبب انقطاع أو إعادة محاولة يدوية)، يتعرف Foodics على المعرف ويرفض التكرار بدلاً من إنشاء حركة مضاعفة في المخزون.

---

## 4. الأثر على وثيقة التصميم الأساسية

هذه الإضافات تُدمج في الجداول التالية من الوثيقة الأصلية:

| الجدول الأصلي | التعديل المطلوب |
|---|---|
| `purchase_transactions` / `transfer_orders` | إضافة الحالة `validation_failed` قبل `submitted` |
| `foodics_sync_log` | إضافة الحقول: `idempotency_key`, `retry_count`, `queue_status`, `translated_error_ar` |
| جدول `items` المحلي | إضافة الحقول: `last_synced_at`, `active_branches` (قائمة الفروع المفعّل بها الصنف) |
| تصميم الشاشات | إضافة "وضع المسح المستمر" كخيار في شاشة إدخال بنود أي عملية (شراء/تحويل/جرد) |

---

## 5. خطوة تنفيذية فورية إضافية

عند طلب صلاحيات API من دعم Foodics، يُطلب تحديداً توضيح:

- حد معدل الطلبات (Rate Limit) لكل دقيقة/ساعة، لضبط سرعة الطابور الخلفي.
- هل يدعم Foodics API خاصية Idempotency Key بشكل أصلي، أم يجب بناء هذا المنطق داخلياً في قاعدة بياناتنا.
- قائمة رموز الأخطاء الكاملة (Error Codes) الخاصة بواجهات Purchasing وTransfer وAdjustment، لبناء قاموس الترجمة العربي بدقة كاملة قبل الإطلاق.
