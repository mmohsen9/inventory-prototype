# وثيقة تصميم نظام إدخال حركات المخزون (Mobile / iPad Web App)
**الجهة:** مساكن رفاء | **الربط:** Foodics API + Google Drive | **الإصدار:** 1.0

---

## 1. نظرة عامة على الحل

نظام ويب متجاوب (Responsive Web App) يعمل من الجوال والآيباد بنفس الرابط، يتيح لأمين المستودع وموظفي الفروع تسجيل عمليات الشراء والتحويل والاستلام والجرد لحظياً عبر مسح Barcode أو كود SKU، مع مراجعة واعتماد من المحاسب قبل الترحيل الآلي إلى Foodics عبر API.

### المبدأ الأساسي
> من يستلم البضاعة يسجلها، ومن يرسل التحويل يسجل خروجه، ومن يستلم التحويل يؤكد دخوله، والمحاسب يراجع ويعتمد ويرحّل.

---

## 2. القرارات المعتمدة

| # | القرار |
|---|---|
| 1 | حساب مستخدم مستقل لكل موظف (صلاحيات محددة حسب الدور) |
| 2 | المواقع: مستودع رئيسي + الفروع (رفاء، التخصصي، هيتن، الصحافة، النزهة، الياسمين) |
| 3 | فاتورة الشراء تُدخل من أمين المستودع، ويعتمدها المحاسب قبل الترحيل |
| 4 | دعم عدة صور لكل فاتورة أو سند |
| 5 | تسجيل 4 كميات لكل تحويل: مطلوبة / مصروفة / مستلمة / فرق |
| 6 | يمكن إغلاق التحويل مع وجود فرق، لكن يبقى بحالة "يحتاج مراجعة" حتى اعتماد المحاسب |
| 7 | الصنف الجديد يُنشأ كـ"طلب صنف" يعتمده المحاسب قبل تفعيله في Foodics |
| 8 | الترحيل لـ Foodics عبر زر يدوي يضغطه المحاسب بعد المراجعة (وليس تلقائياً) |
| 9 | الواجهة متجاوبة: تعمل من الجوال والآيباد بنفس الصفحة دون تطبيقين منفصلين |

---

## 3. الأدوار والصلاحيات

| الدور | الصلاحيات |
|---|---|
| أمين المستودع | إدخال مشتريات المستودع، إنشاء تحويلات، طلب صنف جديد |
| موظف الفرع | إدخال مشتريات الفرع، تأكيد استلام التحويلات، المشاركة في الجرد |
| المحاسب | مراجعة واعتماد جميع العمليات، اعتماد الأصناف الجديدة، الترحيل إلى Foodics، تسوية الفروقات |
| مدير العمليات (اختياري) | عرض تقارير شاملة، لا يدخل عمليات |

---

## 4. هيكل قاعدة البيانات (الجداول الرئيسية)

### 4.1 جدول المستخدمين `users`
| الحقل | النوع | ملاحظات |
|---|---|---|
| user_id | PK | تلقائي |
| name | نص | اسم الموظف |
| role | نص | warehouse / branch_staff / accountant |
| branch_id | FK | الموقع المرتبط بالمستخدم |
| phone | نص | لتسجيل الدخول |
| active | Boolean | تفعيل/تعطيل |

### 4.2 جدول المواقع `locations`
| الحقل | النوع |
|---|---|
| location_id | PK |
| name_ar / name_en | نص |
| type | warehouse / branch |

### 4.3 جدول الأصناف `items` (نسخة محلية مرتبطة بـ Foodics)
| الحقل | النوع | ملاحظات |
|---|---|---|
| item_id | PK | يطابق Foodics id |
| sku | نص | فريد |
| name_en / name_ar | نص | |
| storage_unit | نص | |
| ingredient_unit | نص | |
| storage_to_ingredient_factor | رقم | |
| cost | رقم | من Foodics |
| barcode | نص | قد يكون فارغاً |
| category_reference | نص | |
| sync_status | نص | synced / pending |

> **ملاحظة من ملف الأصناف الفعلي:** 253 صنفاً، منها 75 صنفاً بدون تصنيف category_reference، و48 صنفاً بدون Barcode. يوصى بحملة تصحيح بيانات أولية لإكمال Barcode للأصناف الأكثر حركة قبل الإطلاق. [file:57]

### 4.4 جدول طلبات الأصناف الجديدة `new_item_requests`
| الحقل | النوع |
|---|---|
| request_id | PK |
| requested_by | FK users |
| name_ar / name_en | نص |
| unit | نص |
| conversion_factor | رقم |
| estimated_cost | رقم |
| supplier | نص |
| attachment_url | رابط Google Drive |
| status | pending / approved / rejected |
| approved_by | FK users |
| foodics_sku | نص (يُملأ بعد الاعتماد) |

### 4.5 جدول عمليات الشراء `purchase_transactions`
| الحقل | النوع |
|---|---|
| purchase_id | PK |
| location_id | FK |
| supplier_name | نص |
| invoice_number | نص |
| invoice_date | تاريخ |
| created_by | FK users |
| attachments | مصفوفة روابط Drive |
| status | draft / submitted / approved / posted / rejected |
| posted_to_foodics_at | تاريخ/وقت |
| foodics_reference | نص |

### 4.6 جدول بنود الشراء `purchase_lines`
| الحقل | النوع |
|---|---|
| line_id | PK |
| purchase_id | FK |
| item_id | FK |
| quantity | رقم |
| unit_cost | رقم |
| total_cost | رقم (محسوب) |

### 4.7 جدول التحويلات `transfer_orders`
| الحقل | النوع |
|---|---|
| transfer_id | PK |
| from_location | FK |
| to_location | FK |
| created_by | FK users |
| created_at | تاريخ/وقت |
| confirmed_by | FK users (موظف الفرع) |
| confirmed_at | تاريخ/وقت |
| status | draft / sent / in_transit / received / needs_review / closed |
| foodics_reference | نص |

### 4.8 جدول بنود التحويل `transfer_lines`
| الحقل | النوع |
|---|---|
| line_id | PK |
| transfer_id | FK |
| item_id | FK |
| qty_requested | رقم |
| qty_sent | رقم |
| qty_received | رقم |
| variance_qty | رقم (محسوب: received - sent) |
| variance_reason | نص |

### 4.9 جدول الجرد `count_sessions` و `count_lines`
مطابق لمنطق أعمدة تقرير الجرد الحالي (quantity, originalquantity, variancequantity, variancecost) المستخدم فعلياً في ملفات الشركة. [file:1]

| الحقل | النوع |
|---|---|
| count_id | PK |
| location_id | FK |
| count_date | تاريخ |
| status | open / pending_review / approved / posted |
| item_id | FK (في count_lines) |
| book_quantity | رقم |
| counted_quantity | رقم |
| variance_quantity | رقم |
| variance_cost | رقم |

### 4.10 جدول سجل الترحيل `foodics_sync_log`
| الحقل | النوع |
|---|---|
| log_id | PK |
| source_type | purchase / transfer / count / adjustment |
| source_id | FK |
| synced_by | FK users |
| synced_at | تاريخ/وقت |
| api_status | success / failed |
| error_message | نص |

---

## 5. دورة حالات كل عملية (State Machine)

### 5.1 المشتريات
```
مسودة (Draft) → مُرسلة للمراجعة (Submitted) → معتمدة (Approved) → مرحّلة (Posted)
                                              ↳ مرفوضة (Rejected) → تعاد للمسودة
```

### 5.2 التحويل
```
مسودة → مرسلة (Sent) → قيد النقل (In Transit) → مستلمة (Received)
                                                 ↳ بفرق → تحتاج مراجعة (Needs Review) → معتمدة → مغلقة (Closed)
```

### 5.3 الجرد
```
مفتوح (Open) → قيد المراجعة (Pending Review) → معتمد (Approved) → مرحّل (Posted)
```

### 5.4 طلب صنف جديد
```
معلّق (Pending) → معتمد (Approved) → مُنشأ في Foodics (Synced)
                 ↳ مرفوض (Rejected)
```

---

## 6. تدفق العمل التفصيلي

### 6.1 سيناريو المشتريات
1. أمين المستودع/موظف الفرع يفتح شاشة "مشتريات جديدة".
2. يختار الموقع (يُعبأ تلقائياً حسب صلاحية المستخدم).
3. يصوّر الفاتورة (صورة واحدة أو أكثر) → رفع تلقائي إلى Google Drive في مجلد `Purchases/{location}/{date}`.
4. يدخل: اسم المورد، رقم الفاتورة، التاريخ.
5. لكل صنف:
   - يمسح Barcode بالكاميرا، أو يبحث بـ SKU/الاسم إذا لم يوجد Barcode.
   - إذا وُجد الصنف: تُعبأ الوحدة والتكلفة تلقائياً من قائمة الأسعار المحلية (المتزامنة من Foodics).
   - إذا لم يوجد: يفتح نموذج "طلب صنف جديد" مصغّر، يُحفظ العنصر مؤقتاً في الفاتورة بحالة "قيد اعتماد الصنف".
   - يُدخل الكمية يدوياً.
6. يحسب النظام الإجمالي تلقائياً.
7. يحفظ كـ"مسودة"، ثم يضغط "إرسال للمراجعة".
8. يصل إشعار للمحاسب.
9. المحاسب يراجع الفاتورة والصور والبنود، ثم يعتمد أو يرفض مع سبب.
10. بعد الاعتماد، يضغط المحاسب "ترحيل إلى Foodics" → استدعاء Purchasing API → تسجيل النتيجة في `foodics_sync_log`.

### 6.2 سيناريو التحويل
1. أمين المستودع يفتح "تحويل جديد" ويحدد الفرع المستلم.
2. يمسح كل صنف ويُدخل **الكمية المصروفة فعلياً**.
3. يضغط "إرسال" → تتغير حالة التحويل إلى "قيد النقل"، وتُخفّض كمية المستودع مؤقتاً في العرض (Pending Deduction) حتى الترحيل الفعلي.
4. يصل إشعار فوري لموظف الفرع (عبر إشعار داخل الصفحة أو رسالة).
5. عند وصول البضاعة، يفتح موظف الفرع "استلام تحويل" من جهاز الفرع (جوال أو آيباد).
6. يمسح كل صنف ويؤكد أو يعدّل **الكمية المستلمة**.
7. النظام يحسب الفرق تلقائياً (`qty_received - qty_sent`).
8. إن لم يوجد فرق → الحالة "مستلمة" ثم "مغلقة" مباشرة.
9. إن وجد فرق → الحالة "تحتاج مراجعة"، ويُطلب من الموظف اختيار سبب مبدئي (تلف، نقص، خطأ عد).
10. المحاسب يراجع الفرق، يعتمد السبب النهائي، ثم يرحّل الحركة إلى Foodics (Transfer Order + Adjustment إن لزم).

### 6.3 سيناريو الجرد الدوري
1. يُنشئ المحاسب أو مدير الفرع "جلسة جرد" لموقع وتاريخ محدد (متوافقة مع خطة الجرد الدوري A/B/C المتفق عليها سابقاً).
2. الموظف يمسح كل صنف ضمن قائمة الجرد ويدخل الكمية الفعلية (دون إظهار الرصيد الدفتري لتفادي التحيز).
3. عند إغلاق الجلسة، يُظهر النظام الفروقات (كمية وقيمة) بنفس منطق الأعمدة الحالية: quantity / originalquantity / variancequantity / variancecost. [file:1]
4. المحاسب يراجع الفروقات الكبيرة، ويعتمد التسوية.
5. الترحيل إلى Foodics كـ Inventory Adjustment.

### 6.4 سيناريو الصنف الجديد
1. أثناء أي عملية، إذا لم يُعثر على الصنف بالـ Barcode أو SKU، يظهر زر "طلب صنف جديد".
2. يملأ الموظف: الاسم عربي/إنجليزي، الوحدة، معامل التحويل، التكلفة التقديرية، المورد، صورة الصنف أو الفاتورة.
3. يُحفظ الطلب بحالة "معلّق"، ويستمر الموظف في باقي الفاتورة (يُشار للصنف بأنه "قيد الاعتماد").
4. المحاسب يراجع الطلب، ويحدد نمط الكود الجديد بنفس تسلسل الأكواد الحالي (مثل sk-XXXX أو Op-XXXX حسب الفئة).
5. بعد الاعتماد، يُنشأ الصنف في Foodics عبر API، ويُحدَّث `foodics_sku` في الطلب، ويرتبط تلقائياً ببند الفاتورة المعلّق.

---

## 7. تصميم الشاشات (Responsive)

### مبدأ التجاوب
- **شاشة الجوال (≤480px):** خطوة واحدة في كل شاشة (Wizard Steps) — مناسبة لأمين المستودع أثناء الحركة.
- **شاشة الآيباد (≥768px):** عرض جدول كامل يشبه واجهة كاشير (POS Grid) — مناسب للمراجعة والاعتماد.
- تصميم واحد (CSS Responsive) بدون نسخ منفصلة، لتفادي ازدواج الصيانة.

### شاشات النظام
| الشاشة | الجوال | الآيباد |
|---|---|---|
| تسجيل الدخول | نموذج مبسط | نفس النموذج، أوسع |
| القائمة الرئيسية | أيقونات كبيرة (مشتريات / تحويل / استلام / جرد) | شريط جانبي + لوحة رئيسية |
| مسح الصنف | كاميرا ملء الشاشة | كاميرا أو ماسح خارجي Bluetooth |
| إدخال الكمية | لوحة أرقام كبيرة | حقل جدول قابل للتعديل مباشرة |
| مراجعة الفاتورة | تمرير رأسي للبنود | جدول كامل مع الصور بجانبه |
| لوحة المحاسب | غير مخصصة أساساً للجوال | جدول اعتماد + فلاتر + أزرار ترحيل |
| الإشعارات | Badge على الأيقونة | لوحة إشعارات جانبية |

---

## 8. التكامل مع Foodics API

| العملية | Endpoint المطلوب من Foodics (يُطلب من الدعم) | الاتجاه |
|---|---|---|
| مزامنة الأصناف | Inventory Items | سحب دوري (كل ساعة) |
| ترحيل المشتريات | Purchasing Transactions | إرسال بعد الاعتماد |
| ترحيل التحويلات | Transfer Orders | إرسال بعد الاعتماد |
| ترحيل التسويات | Inventory Adjustments | إرسال بعد الاعتماد |
| إنشاء صنف جديد | Create Inventory Item | إرسال بعد اعتماد المحاسب |

**خطوة عملية فورية:** التواصل مع دعم Foodics لطلب تفعيل API Access وHawk/Token الخاص بالحساب، وتحديد الصلاحيات (قراءة الأصناف، كتابة المشتريات والتحويلات والتسويات).

---

## 9. تخزين الصور (Google Drive)

هيكل المجلدات المقترح:
```
/Rafa-Inventory-System/
  /Purchases/{location}/{yyyy-mm}/{invoice_id}/
  /Transfers/{transfer_id}/sent/
  /Transfers/{transfer_id}/received/
  /NewItemRequests/{request_id}/
  /CountSessions/{count_id}/
```
- رفع تلقائي عند التقاط الصورة من المتصفح (Google Drive API أو رفع عبر رابط مجلد مشترك).
- كل سجل في قاعدة البيانات يحفظ رابط الملف فقط، وليس الصورة نفسها.

---

## 10. المؤشرات والتقارير التشغيلية

| التقرير | الغرض |
|---|---|
| عمليات معلّقة أكثر من 24 ساعة | متابعة الالتزام بالتوقيت |
| تحويلات لم تُستلم | كشف تأخر الفروع |
| فروقات الجرد حسب الفرع | رقابة دقة المخزون |
| طلبات أصناف جديدة معلّقة | تسريع الاعتماد |
| سجل فشل الترحيل لـ Foodics | معالجة الأخطاء التقنية |

---

## 11. خطة التنفيذ المرحلية

| المرحلة | المدة التقديرية | المخرجات |
|---|---|---|
| 1. طلب API من Foodics + تجهيز قاعدة البيانات | أسبوع | صلاحيات API جاهزة |
| 2. بناء شاشات المشتريات والتحويل (نسخة أولية) | 2-3 أسابيع | نموذج قابل للتجربة |
| 3. اختبار داخلي مع أمين المستودع وفرع واحد | أسبوعان | تصحيح الأخطاء العملية |
| 4. إضافة الجرد وطلب الأصناف الجديدة | أسبوعان | تغطية كاملة للعمليات |
| 5. تفعيل الترحيل الفعلي إلى Foodics | أسبوع | ربط حي مع النظام المحاسبي |
| 6. تعميم على جميع الفروع | أسبوعان | تشغيل كامل |

---

## 12. ملاحظات جودة البيانات المسبقة

بمراجعة ملف الأصناف الفعلي (253 صنفاً): [file:57]
- 48 صنفاً بدون Barcode — يوصى بطباعة ملصقات قبل الإطلاق للأصناف الأكثر حركة.
- 75 صنفاً بدون تصنيف (category_reference) — لا يمنع الإطلاق لكنه يُضعف التقارير التحليلية لاحقاً.
- بعض الأصناف بأسماء متشابهة أو بدون اسم عربي — يوصى بمراجعة أسماء الأصناف مع المحاسب قبل الربط النهائي لتفادي التباس أثناء المسح.
